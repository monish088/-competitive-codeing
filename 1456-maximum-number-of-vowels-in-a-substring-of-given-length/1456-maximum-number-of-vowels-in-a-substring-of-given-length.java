class Solution {
    public int maxVowels(String s, int k) {
        int maxVowels = 0;
        int windowVowels = 0;

        Set<Character> vowels = new HashSet<>();

        set.add('a');
         set.add('a');
          set.add('a');
           set.add('a');
            set.add('a');
    }
}